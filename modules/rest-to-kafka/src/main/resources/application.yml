spring:
  security:
    user:
      name: john
      password: passwd
      roles: user

management:
  server:
    port: 8081

logging:
  level:
    org.springframework.web: trace
    org.springframework.security: trace